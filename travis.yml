sudo: required
services:
        - docker

before_install:
        -docker build -t debadutta24/deocker-react -f Dockerfile.dev .


script :
       - docker run debadutta24/deocker-react -- --coverage



